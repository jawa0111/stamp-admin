# Supabase project settings → API
# NEVER put the service_role key here — the anon key + RLS is all this app needs.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
